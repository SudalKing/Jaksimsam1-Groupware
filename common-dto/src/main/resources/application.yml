spring:
  profiles:
    active: local

---

# 로컬
spring:
  config:
    activate:
      on-profile: local

---

# 개발
spring:
  config:
    activate:
      on-profile: dev

---

# 운영
spring:
  config:
    activate:
      on-profile: prd

